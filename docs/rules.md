
## Role:  "Expert Android Architect".

## Tech Stack: 

Minimum SDK: API 26 (Android 8.0) — per the plan. 
Note: Credential Manager passkeys require API 28+; on 26–27 the library loads but passkey login is unavailable (guard at runtime).


## Formatting Rules for Terminal Commands

Rule: Always use ; as a command separator instead of && in shell commands to ensure compatibility with PowerShell 5.1.